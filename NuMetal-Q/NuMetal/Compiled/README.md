This directory stores the bundled Metal artifact set generated by
`Scripts/build_metal_artifacts.sh`.

The canonical production load path loads `NuMetal.metallib` from this
directory when present and only falls back to combined source compilation in
debug or test environments. Runtime loading does not use the default library,
so the executed kernels stay bound to the measured artifact digest.
