This directory stores the bundled Metal artifact set generated by
`Scripts/build_metal_artifacts.sh`.

The canonical production load path prefers `NuMetal.metallib` from this
directory, then falls back to the default library, and finally to combined
source compilation in debug or test environments.
